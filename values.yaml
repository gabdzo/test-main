# Helm chart values
image:
  repository: your-registry/your-image  # Replace with your image repository
  tag: initial-tag  # This will be updated by the workflow
  pullPolicy: IfNotPresent

version: initial-version  # This will be updated by the workflow

# Add other Helm chart values as needed 